# 第一篇前端文章

> 这是一篇示例文章，演示如何写博客。把它删掉，换成你自己的内容。

## 写作方式

直接在 `docs/blog/frontend/` 下新建 `.md` 文件即可，文件名就是 URL。

## 代码示例

VitePress 自带代码高亮：

```ts
function greet(name: string): string {
  return `Hello, ${name}!`
}
```

## 提示框

::: tip 提示
可以用这种容器语法做提示框。
:::

::: warning 注意
也支持 warning、danger 等类型。
:::
